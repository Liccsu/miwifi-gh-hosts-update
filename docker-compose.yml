services:
  miwifi-gh-hosts-update:
    image: ghcr.io/liccsu/miwifi-gh-hosts-update:latest
    container_name: miwifi-gh-hosts-update
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=Asia/Shanghai
    volumes:
      # token 缓存 (账号模式下自动刷新后持久化, 避免频繁登录)
      - ./data:/data
